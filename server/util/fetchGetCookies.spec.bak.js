'use strict'
// eslint ignor
const test = require('tape')
const fetch = require('./fetchGetCookies')(require('node-fetch'))

test('add cookies to response when cookies are present')
test('return plain response when cookies are missing')
test('with options')
test('without options')